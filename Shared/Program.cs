namespace Shared
{
    /// <summary>
    /// The namespace Shared contains classes that are used in both server and
    /// client applications an dare the same (to prevent duplicities). It shall
    /// be build as a Dynamic Link Library.
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
